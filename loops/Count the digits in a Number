declare 
    a number := 121;
    sum number := 0;
    rem number ;
begin
    while a>0 loop
       rem := mod(a,10);
       sum := sum + rem;
       a := trunc(a/10);
    end loop;
    dbms_output.put_line(sum);
  end;
  /
